services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.prod
    volumes:
      - ./data/assets:/app/assets

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    volumes: []
